pipeline {
    agent any

    stages {

        stage('Docker Check') {
            steps {
                sh 'docker --version'
                sh 'docker ps'pipeline {
    agent any

    stages {

        stage('Docker Check') {
            steps {
                sh 'docker --version'
                sh 'docker ps'
            }
        }

        stage('Build') {
            steps {
                sh 'echo "Build started"'
                sh 'ls -la'
            }
        }

        stage('Test') {
            steps {
                sh 'echo "Testing application"'
            }
        }
    }
}pipeline {
    agent any

    stages {

        stage('Docker Check') {
            steps {
                sh 'docker --version'
                sh 'docker ps'
            }
        }

        stage('Build') {
            steps {
                sh 'echo "Build started"'
                sh 'ls -la'
            }
        }

        stage('Test') {
            steps {
                sh 'echo "Testing application"'
            }
        }
    }
}pipeline {
    agent any

    stages {

        stage('Docker Check') {
            steps {
                sh 'docker --version'
                sh 'docker ps'
            }
        }

        stage('Build') {
            steps {
                sh 'echo "Build started"'
                sh 'ls -la'
            }
        }

        stage('Test') {
            steps {
                sh 'echo "Testing application"'
            }
        }
    }
}pipeline {
    agent any

    stages {

        stage('Docker Check') {
            steps {
                sh 'docker --version'
                sh 'docker ps'
            }
        }

        stage('Build') {
            steps {
                sh 'echo "Build started"'
                sh 'ls -la'
            }
        }

        stage('Test') {
            steps {
                sh 'echo "Testing application"'
            }
        }
    }
}srgrgrg
            }
        }

        stage('Build') {
            steps {
                sh 'echo "Build started"'
                sh 'ls -la'
            }
        }

        stage('Test') {
            steps {
                sh 'echo "Testing application"'
            }
        }
    }
}
